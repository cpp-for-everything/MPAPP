#pragma once

// MPAPP umbrella header.
//
// This is the canonical entry point for the MPAPP public API. Including this
// header brings the entire user-facing surface of the framework into scope via
// the mpapp namespace. The public API is expressed exclusively through C++
// types (templates, classes, free functions) -- no preprocessor macros are
// part of the public surface (see ADR-0002, ADR-0009).

namespace mpapp {

// Placeholder: subsystem headers will populate this namespace in later tasks.

} // namespace mpapp
