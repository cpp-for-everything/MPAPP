// SPDX-License-Identifier: Apache-2.0
// Part of MPAPP. Cross-platform umbrella header for `page_handler`.
//
// Generated by tools/dev/generate-handler-umbrellas.py (run from repo
// root). Do not edit by hand — re-run the generator after adding or
// removing per-platform handler files.
//
// Usage:
//   #include <mpapp/handlers/page_handler.hpp>
//   mpapp::page_handler<mpapp::platform::current> handler_{};
//
// The umbrella dispatches via `__has_include` to the per-platform header
// matching the build target. Platforms without an implementation yet
// emit a clear `#error` rather than a missing-header diagnostic.

#ifndef MPAPP_HANDLERS_PAGE_HANDLER_HPP
#define MPAPP_HANDLERS_PAGE_HANDLER_HPP

#if defined(_WIN32)
#  if __has_include(<mpapp/handlers/windows/page_handler.hpp>)
#    include <mpapp/handlers/windows/page_handler.hpp>
#  else
#    error "mpapp::page_handler<platform::windows> not implemented yet."
#  endif
#elif defined(__ANDROID__)
#  if __has_include(<mpapp/handlers/android/page_handler.hpp>)
#    include <mpapp/handlers/android/page_handler.hpp>
#  else
#    error "mpapp::page_handler<platform::android> not implemented yet."
#  endif
#elif defined(__APPLE__)
#  include <TargetConditionals.h>
#  if TARGET_OS_IPHONE
#    if __has_include(<mpapp/handlers/ios/page_handler.hpp>)
#      include <mpapp/handlers/ios/page_handler.hpp>
#    else
#      error "mpapp::page_handler<platform::ios> not implemented yet — pending M-08."
#    endif
#  else
#    if __has_include(<mpapp/handlers/macos/page_handler.hpp>)
#      include <mpapp/handlers/macos/page_handler.hpp>
#    else
#      error "mpapp::page_handler<platform::macos> not implemented yet — pending M-07."
#    endif
#  endif
#elif defined(__linux__)
#  if __has_include(<mpapp/handlers/linux/page_handler.hpp>)
#    include <mpapp/handlers/linux/page_handler.hpp>
#  else
#    error "mpapp::page_handler<platform::linux_> not implemented yet."
#  endif
#else
#  error "MPAPP: unsupported host platform — no `mpapp::page_handler` available."
#endif

#endif // MPAPP_HANDLERS_PAGE_HANDLER_HPP
