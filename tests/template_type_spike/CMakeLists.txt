# Template-wrapper-type spike (T-0002). Catch2 + mpapp-core are provided by the
# parent tests/ directory; the helper handles linking, warnings, and discovery.
mpapp_add_catch_test(template_type_spike_test SOURCES test.cpp)
